#include "practice.h"

/**
 * main - Program that adds three numbers
 *
 * Return: 0
 */
int main(void)
{
	int a = 31;
	int b = 22;
	int c = 10;

	int sum = ridwan(a + b + c);

	printf("The sum of %d & %d & %d is: %d\n", a, b, c, sum);
	printf("The sum of %d and %d and %d is: %d\n", a, b, c, sum);
return (0);
}
