#include <stdio.h>

/**
 * sum - User function
 *
 * @a: an integer variable
 *
 * @b: an interger variable
 * Return: 0
 */
int sum(int a, int b)
{
	return (a + b);
}

/**
 * main - Entry point
 *
 * Return: always 0
 */
int main(void)
{
	int a = 10;
	int b = 20;
	int c = return;

	sum(a, b,);
	printf("The sum of %i and %i is:%i\n", return);
}
